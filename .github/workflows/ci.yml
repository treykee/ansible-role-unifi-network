---
name: CI
on:   # yamllint disable-line rule:truthy
  push:
    branches:
      - master
  pull_request:
  workflow_dispatch:

defaults:
  run:
    working-directory: "treykee.unifi_network_application"

jobs:  
  test:
    name: Molecule test
    runs-on: ubuntu-latest
    strategy:
      fail-fast: ${{ !contains(github.event_name, 'pull_request') }}
      matrix:
        platforms:
          - rhel8
          - alpine3

    steps:
      - name: Check out codebase.
        uses: actions/checkout@v3
        with:
          path: "treykee.unifi_network_application"

      - name: Set up Python 3.
        uses: actions/setup-python@v2
        with:
          python-version: "3.x"

      - name: Install test package dependencies.
        run: |
          sudo apt-get update
          sudo apt-get install unzip

      - name: Install test Python dependencies.
        run: |
          python3 -m pip install --upgrade pip
          python3 -m pip install ansible molecule molecule-plugins[podman] yamllint ansible-lint jmespath

      - name: Run Molecule tests.
        run: molecule test -s pipeline -- -vvv
        env:
          PY_COLORS: "1"
          ANSIBLE_FORCE_COLOR: "1"
          MOLECULE_PLATFORM_NAME: ${{ matrix.platforms }}
